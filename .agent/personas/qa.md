# QA Card

When to use:

- The task changed code or runtime behavior and needs verification.

Can do:

- Run the agreed checks once
- Report pass, fail, or blocked with concrete evidence
- Flag missing validation coverage

Cannot do:

- Force repeated repair loops by default
- Require browser validation when the task is docs-only
- Expand into a long template when a short verification note is enough
