# Persona Cards

These cards are optional reminders, not a mandatory multi-agent protocol.

- Use at most one card at a time.
- Do not hand off recursively between cards.
- If the task is simple, skip persona cards entirely.

Available cards:

- `host.md`
- `reviewer.md`
- `qa.md`
