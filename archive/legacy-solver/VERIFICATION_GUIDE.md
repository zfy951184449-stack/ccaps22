# 修复验证指南

## 修复内容

已在 `solver/server.py` 中添加了两处上限约束，防止超额分配：

### 修改1：第350行（统一建模版本）
```python
# 约束：实际分配 + 缺口 >= 需求
model.Add(sum(vars_for_op) + slack_var >= required)

# 新增：约束实际分配人数不超过需求（防止超额分配）
model.Add(sum(vars_for_op) <= required)
```

### 修改2：第1688行（旧版本）
```python
model.Add(sum(vars_for_op) >= required)
# 新增：约束实际分配人数不超过需求（防止超额分配）
model.Add(sum(vars_for_op) <= required)
```

## 如何验证修复

### 方法1：重新运行求解器

1. **重启求解器服务**
   ```bash
   cd /Users/zhengfengyi/ccaps22/solver
   # 如果solver正在运行，先停止它
   # 然后重新启动
   python app.py
   ```

2. **通过前端或API触发新的排班**
   - 创建一个新的生产批次
   - 运行自动排班

3. **检查结果**
   ```bash
   cd /Users/zhengfengyi/ccaps22/backend
   npx ts-node check_allocation.ts
   ```

   期望输出：
   ```
   ✅ 未发现超额分配的操作
   ```

### 方法2：直接检查现有批次

如果想要对比修复前后的差异：

```bash
cd /Users/zhengfengyi/ccaps22/backend
npx ts-node check_all_batches.ts
```

这会显示所有批次的分配情况。修复后生成的新批次应该没有超额分配。

### 方法3：查看求解器日志

求解器在求解过程中会输出日志，可以检查：
- 是否有约束冲突
- 是否成功找到可行解
- 求解时间是否显著增加

## 预期结果

### ✅ 成功标志
- 所有操作的分配人数 ≤ 需求人数
- 没有"超额X人"的提示
- 求解器仍能找到可行解

### ⚠️ 需要关注
- 如果某些操作无法满足需求（分配人数 < 需求），这是合理的（说明人手不足）
- 求解时间可能会略微增加（因为约束更严格）

### 🚨 问题信号
- 求解器频繁返回"无可行解"
- 之前能求解的批次现在失败
- 需要调整其他约束或增加人员

## 后续步骤

如果验证成功：
1. ✅ 关闭相关问题单
2. ✅ 更新文档说明约束逻辑
3. ✅ 考虑添加单元测试防止回归

如果出现问题：
1. 检查求解器日志定位失败原因
2. 可能需要调整其他约束（如松弛变量）
3. 联系开发团队讨论解决方案
