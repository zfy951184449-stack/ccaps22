# deprecated rules mirror

This directory is no longer an active rule source.

- Do not load persona or workflow instructions from `.agents/rules/`.
- Use `../../AGENTS.md` and `../../.agent/index.md` instead.
