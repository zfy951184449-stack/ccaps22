import React from 'react';
import { Card, Empty } from 'antd';

const AutoSchedulingWorkbench: React.FC = () => {
  return (
    <Card>
      <Empty description="自动排班工作台 (开发中 - 已重置)" />
    </Card>
  );
};

export default AutoSchedulingWorkbench;
