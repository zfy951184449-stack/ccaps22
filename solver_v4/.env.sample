# Solver V4 环境变量参考（reference）
#
# ⚠️ 注意：solver 进程直接读 os.environ，并**不会**自动加载本文件。
# 请通过「进程环境」提供这些变量：
#   - 本地：用 ../start_all.sh 启动（会自动从 backend/.env 同步 SOLVER_CALLBACK_SECRET 并 export）；
#   - 容器/平台：用 Dockerfile / Procfile / 平台环境变量配置。
# 本文件仅作为「solver 消费哪些 env + 默认值」的文档与拷贝模板。

# 后端回调地址（进度/结果 POST 与 status 轮询 URL 都基于它推导）。默认指向本地后端。
BACKEND_API_URL=http://localhost:3001/api/v4/scheduling/callback/progress

# solver→backend 回调共享密钥。**必须与 backend/.env 的 SOLVER_CALLBACK_SECRET 完全一致。**
# solver 在「进度 POST / 结果 POST / status 轮询 GET」上发送 header
#   X-Solver-Callback-Token: <该值>
# 由 backend 的 requireServiceAuth 校验。缺失或不匹配 → backend 返回 401，进度/结果无法回写。
# 生成：openssl rand -hex 32
SOLVER_CALLBACK_SECRET=replace-with-openssl-rand-hex-32

# 其它已被 solver 读取的开关（列出供参考，非本次新增）：
# PORT / SOLVER_V4_PORT   求解器监听端口（默认 5005）
# SOLVER_WORKERS          CP-SAT 工作线程数（默认 min(8, CPU 核数)）
# SOLVER_DEBUG            =1 打开 CP-SAT 搜索日志
# FLASK_DEBUG             =1 Flask debug（仅 `python app.py` 直跑时生效）
